<?xml version="1.0" encoding="utf-8"?>
<navigation
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/nav_graph"
    app:startDestination="@id/recipesFragment">

    <fragment
        android:id="@+id/recipesFragment"
        android:name="il.ac.tcb.smartchef.ui.RecipesFragment"
        android:label="Recipes" >
        <action
            android:id="@+id/action_recipesFragment_to_recipeDetailFragment"
            app:destination="@id/recipeDetailFragment"/>
    </fragment>
    <fragment
        android:id="@+id/recipeDetailFragment"
        android:name="il.ac.tcb.smartchef.ui.RecipeDetailFragment"
        android:label="Recipe detail" >
        <argument
            android:name="recipeId"
            app:argType="string" />
    </fragment>
    <fragment
        android:id="@+id/shoppingFragment"
        android:name="il.ac.tcb.smartchef.ui.ShoppingFragment"
        android:label="Shopping" />

    <fragment
        android:id="@+id/converterFragment"
        android:name="il.ac.tcb.smartchef.ui.ConverterFragment"
        android:label="Converter" />

    <fragment
        android:id="@+id/profileFragment"
        android:name="il.ac.tcb.smartchef.ui.ProfileFragment"
        android:label="Profile" />

</navigation>
